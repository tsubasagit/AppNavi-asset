# グラフコンポーネント Pro

高度なデータ可視化を実現するAppNaviプラグインです。

## 機能

- 📊 **複数のグラフタイプ**: 線グラフ、棒グラフ、円グラフ、ドーナツグラフ、レーダーチャート
- 🔍 **データ検索**: グラフデータ内の検索機能
- ⚡ **リアルタイム更新**: データの動的更新に対応
- 🎨 **カスタマイズ可能**: Chart.jsの全オプションに対応
- 📱 **レスポンシブ**: モバイルデバイスにも対応

## インストール

```bash
npm install @appnavi/chart-graph-plugin chart.js react-chartjs-2
```

## 使用方法

```tsx
import { ChartGraphPlugin } from '@appnavi/chart-graph-plugin'

function App() {
  const data = {
    labels: ['1月', '2月', '3月', '4月', '5月'],
    datasets: [
      {
        label: '売上',
        data: [120, 190, 300, 500, 200],
        backgroundColor: 'rgba(102, 126, 234, 0.2)',
        borderColor: 'rgba(102, 126, 234, 1)',
      }
    ]
  }

  return (
    <ChartGraphPlugin
      type="line"
      data={data}
      height={400}
      showSearch={true}
      options={{
        plugins: {
          title: {
            display: true,
            text: '月次売上'
          }
        }
      }}
    />
  )
}
```

## Props

| プロパティ | 型 | デフォルト | 説明 |
|---------|-----|---------|------|
| type | 'line' \| 'bar' \| 'pie' \| 'doughnut' \| 'radar' | 'line' | グラフのタイプ |
| data | object | required | Chart.js形式のデータ |
| options | object | {} | Chart.jsのオプション |
| height | number | 400 | グラフの高さ（ピクセル） |
| showSearch | boolean | true | 検索機能を表示するか |

## プレビュー

プレビュー機能を使用して、プラグインの動作を確認できます。

```bash
# プレビューを開く
open preview.html
```

## ライセンス

MIT License

